<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Web School</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>
    <link rel="stylesheet" type="text/css" href="../css/index-style.css">
  </head>
  <body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <a class="navbar-brand" href="/">Web School</a>
    </nav>

    <div class="container mt-5">
      <a href="/teachers/add" class="btn btn-primary">Add Teacher</a>
      <div class="table-responsive-sm">
        <table class="table mt-2">
          <thead class="thead bg-info text-white">
            <tr>
              <th>No</th>
              <th>First name</th>
              <th>Last Name</th>
              <th>Email</th>
              <th>Subject</th>
              <th>Action</th>
            </tr>
          </thead>

          <tbody>
            <% Teachers.forEach((teacher, i) => { %>
              <tr>
                <td><%= i+1 %></td>
                <td> <%= teacher.first_name%> </td>
                <td> <%= teacher.last_name%> </td>
                <td> <%= teacher.email %> </td>
                <td> <%= teacherHelpers.assignSubject(teacher.Subject) %> </td>
                <td>
                  <a href='/teachers/edit/<%= teacher.id %>'>Edit</a> |
                  <a class="text-danger" href='/teachers/delete/<%= teacher.id %>' onclick="return confirm('delete this teacher?')"> delete</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
